import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SocialPost = () => {
  return <View style={styles.postDiv}></View>;
};

export default SocialPost;

const styles = StyleSheet.create({
  postDiv: {
    flex: 1,
  },
});
